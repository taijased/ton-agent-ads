{
  "name": "@repo/agent-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "lint": "pnpm --dir ../.. exec prettier --check \"apps/agent/package.json\" \"apps/agent/tsconfig.json\" \"apps/agent/src/**/*.ts\"",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --eval \"console.log('No tests for @repo/agent-app')\"",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
