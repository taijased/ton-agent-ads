# TypeScript Style

Use this prompt when working in TypeScript code or TypeScript design docs.

Prefer:
- clear types at boundaries
- explicit interfaces for shared contracts when the codebase already uses them
- consistency with existing status constants, DTOs, and repository interfaces
- readable naming over shorthand

When documenting TypeScript work:
- reference the actual exported types and modules in the repository
- make app-local versus shared type ownership explicit
